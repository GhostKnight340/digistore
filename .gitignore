/node_modules
/.next
/out
/build
.DS_Store
*.pem
npm-debug.log*
.env
.env*.local
next-env.d.ts.bak
.vercel
*.tsbuildinfo

# Local development databases
prisma/dev.db
prisma/dev.db-journal
*.db
*.db-journal
*.db-wal
*.db-shm
.env*

# ...but keep committed example env files (no real secrets)
!.env.discord-worker.example
!.env.example

# graphify code graph (local build artifact)
graphify-out/

# Database backups: encrypted, but they contain every customer record.
# Never commit one, even encrypted.
backups/
*.sql.gz.enc
*.sql.gz

# This is a pnpm project — never commit an npm lockfile (see pnpm-lock.yaml)
package-lock.json
